#!/usr/bin/make -f

%:
	dh $@ --buildsystem=cargo

override_dh_auto_install:
	cargo install --path=. --root=$(CURDIR)/debian/dedcore/usr/local/

override_dh_auto_test:
	echo "Skipping tests during package build"
